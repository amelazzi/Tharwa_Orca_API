/*var expect = require('chai').expect;
var mocha = require('mocha')
var describe = mocha.describe*/

/*module.exports = function(compteAccess){
describe('créer un nouveau compte banquaire ', function () {
        it('should return true (new bank account created)', function () {
          
          // 1. ARRANGE
          var idClient = 'testUnitaire1@mailinator.com';
          var codeMonnaie = 'USD';
          var TypeCompte = 3;
      
          // 2. ACT
          compteAccess.CreateBanqueAccount(idClient,codeMonnaie,TypeCompte,function(isCreated) {
                      
              // 3. ASSERT
               expect(isCreated).to.be.equal(false);
             });
      
          
      
        });
 });



describe('construire le prochaine numéro de compte', function () {
    it('should return a valide numAccount', function () {
      
      // 1. ARRANGE
      var middleNum = '15';
      var codeMonnaie = 'DZD';
  
      // 2. ACT
      numCompte = compteAccess.makeNumAccount(middleNum,codeMonnaie);
      //3. Assert
      expect(numCompte).to.be.equal("THW000015DZD"); 
  
      
  
    });
});
    
}*/